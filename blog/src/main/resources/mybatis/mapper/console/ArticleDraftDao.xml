<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sbsm.blog.dao.console.ArticleDraftDao">


    <insert id="insert" parameterType="com.sbsm.blog.entity.console.ArticleDraft">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
        INSERT INTO `article_draft` (  `create_time`, `update_time`, `remark`, `del_flag`, `ar_id`, `title`, `descn`, `content`, `img_url`, `ad_id`, `is_new` )
        VALUES
                                    (#{createTime}, #{updateTime}, #{remark}, #{delFlag}, #{arId}, #{title}, #{descn}, #{content}, #{imgUrl}, #{adId}, #{isNew} )
    </insert>

    <update id="update" parameterType="com.sbsm.blog.entity.console.ArticleDraft">
        UPDATE `article_draft`
        SET 
        `update_time` = #{updateTime},
        `remark` = #{remark},
        `title` = #{title},
        `descn` = #{descn},
        `img_url` = #{imgUrl}
        WHERE
            `id` = #{id};
    </update>

    <update id="updateContent" parameterType="com.sbsm.blog.entity.console.ArticleDraft">
        UPDATE `article_draft`
        SET
        `update_time` = #{updateTime},
        `content` = #{content}
        WHERE
            `id` = #{id}
    </update>

    <update id="delete">
         UPDATE `article_draft`
         SET
         `update_time` = now(),
         `del_flag`=true
        WHERE
         `id` = #{arg0}
    </update>

    <sql id="columns">
        `create_time` createTime, `update_time` updateTime, `is_new` isNew, `ar_id` arId, `ad_id` adId
    </sql>

    <select id="countByDelFlag" resultType="Integer">
        select count(*) from `article_draft` where  `del_flag`=#{arg0}
    </select>

    <select id="findPage" resultType="com.sbsm.blog.entity.console.ArticleDraft">
        select *, <include refid="columns" /> from `article_draft` where  `del_flag`=0
        order by `update_time` desc
        limit #{arg0}, #{arg1}
    </select>

    <select id="findOne" resultType="com.sbsm.blog.entity.console.ArticleDraft">
        select *, <include refid="columns" /> from `article_draft`
        <where>
            <if test="id != null">
                and `id` = #{id}
            </if>
        </where>
        limit 0,1
    </select>

    <select id="findByIds" resultType="com.sbsm.blog.entity.console.ArticleDraft">
        select *, <include refid="columns" /> from `article_draft`
        where `id` in
        <foreach collection="array" open="(" separator="," close=")" item="item">
            #{item}
        </foreach>
    </select>

</mapper>